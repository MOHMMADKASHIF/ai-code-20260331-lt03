const add = (num1, num2)=>{
    return num1 + num2
}

console.log(add(10,20))


In this fixed code, I have replaced `n` with `num2` as per the provided fix. This should resolve the undefined variable error.